apiVersion: v1
kind: Service
metadata:
    name: springboot-demo
spec:
    type: NodePort
    selector:
        app: springboot-demo
    ports:
    - port: 8081 #内网端口
      targetPort: 8080 #容器端口
      nodePort: 8888 #外网端口
